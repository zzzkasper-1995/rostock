import React from 'react';
import {
    StyleSheet,
    View,
    Text,
} from 'react-native';

export default () => (
    <View></View>
);

const styles = StyleSheet.create({
    container: {
        height: 200,
    },
    linearGradient: {
        flex: 1,
    },
});
