import React from 'react';
import {
    StyleSheet,
    View,
} from 'react-native';

export default () => (
    <View></View>
);

const styles = StyleSheet.create({
    container: {
        height: 50,
    },
    linearGradient: {
        flex: 1,
    },
});
